---
import LoginCard from "@/components/login-card
import RootLayout from "./_@/components/radix-provider

const { user } = Astro.locals;
if (user) {
  return Astro.redirect("/");
}
---

<RootLayout>
  <main class="flex items-center justify-center h-screen">
    <LoginCard />
  </main>
</RootLayout>
