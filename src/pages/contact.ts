import type { APIRoute } from "astro";

export const POST: APIRoute = async () => {
  // get form data
  // check that the form data is valid
  // lookup which project the form data is for
  // send email to project owner
  // send email to user
  // return either redirect or json response

  return new Response();
};
